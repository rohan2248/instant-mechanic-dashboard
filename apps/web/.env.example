# Base URL of the Express API, including the /api prefix.
# Server-only on purpose: the browser never calls the API directly, so this
# must NOT be prefixed with NEXT_PUBLIC_.
API_BASE_URL="http://localhost:4000/api"

# Optional. How long a single API call may block a server render.
# API_TIMEOUT_MS=10000
